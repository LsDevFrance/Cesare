/*
** EPITECH PROJECT, 2022
** MY HUNTER
** File description:
** i love epitech it is so cool, the AER are the best!
*/

#ifndef MYLIST_
    #define MYLIST_

    #include <stdio.h>
    #include <stdlib.h>
    #include <unistd.h>

    char * str_copy(char * str, int start, int end);
    char * str_add(char * str1, char * str2);
    int str_len(char *str);
    void my_put_str(char *str);

#endif
